<ul>
  <% @users.each do |user| %>
  <li> <img src='<%=user.photo%>'>
    <div>
      <p> <%= link_to user.name, user_path(user) %> </p>
      <p> Number of posts: <%= link_to user.post_counter, user_path(user) %> </p>
    </div>
    
  </li>
  <% end %>
</ul>